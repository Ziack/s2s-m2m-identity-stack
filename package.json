{
  "name": "s2s-m2m-identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "packages/examples/*", "packages/services/*"],
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "npm -ws run build",
    "test": "npm -ws run test",
    "infra:validate": "cd infrastructure/terraform && terraform fmt -check -recursive && terraform validate"
  }
}
