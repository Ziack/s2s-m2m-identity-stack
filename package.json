{
  "name": "s2s-m2m-identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/examples/*",
    "examples/basic/*",
    "examples/chained/*",
    "examples/multi-context/*",
    "examples/chained/e2e",
    "examples/migrations/from-passport-jwt/before",
    "examples/migrations/from-passport-jwt/after",
    "examples/migrations/from-casbin-rbac/before",
    "examples/migrations/from-casbin-rbac/after",
    "examples/migrations/from-no-auth/before",
    "examples/migrations/from-no-auth/after"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm -ws run build",
    "test": "npm -ws run test",
    "infra:validate": "cd infrastructure/terraform && terraform fmt -check -recursive && terraform validate"
  }
}
